[package]
name = "ghostkernel-guardian"
version = "0.1.0"
edition = "2021"

[dependencies]
gk-audit = { path = "../../libs/gk-audit" }
ghostkernel-netd = { path = "../ghostkernel-netd" }
ghostkernel-shardd = { path = "../ghostkernel-shardd" }
thiserror = "1.0"

