#!/bin/sh
set -e

if [ "$1" = "configure" ]; then
    systemctl daemon-reload || true
    systemctl enable ghostkernel-guardian.service || true
    systemctl start ghostkernel-guardian.service || true
fi

#DEBHELPER#

exit 0
