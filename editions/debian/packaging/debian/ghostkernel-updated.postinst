#!/bin/sh
set -e

if [ "$1" = "configure" ]; then
    systemctl daemon-reload || true
    systemctl enable ghostkernel-updated.service || true
    systemctl start ghostkernel-updated.service || true
fi

#DEBHELPER#

exit 0
