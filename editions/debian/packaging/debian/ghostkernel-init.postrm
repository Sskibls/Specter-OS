#!/bin/sh
set -e

if [ "$1" = "remove" ]; then
    systemctl stop ghostkernel-init.service || true
    systemctl disable ghostkernel-init.service || true
fi

if [ "$1" = "purge" ]; then
    systemctl daemon-reload || true
fi

#DEBHELPER#

exit 0
