#!/bin/sh
set -e

if [ "$1" = "configure" ]; then
    systemctl daemon-reload || true
    systemctl enable ghostkernel-init.service || true
    systemctl start ghostkernel-init.service || true
fi

#DEBHELPER#

exit 0
