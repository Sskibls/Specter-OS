#!/bin/sh
set -e

if [ "$1" = "remove" ]; then
    systemctl stop ghostkernel-policyd.service || true
    systemctl disable ghostkernel-policyd.service || true
fi

if [ "$1" = "purge" ]; then
    systemctl daemon-reload || true
fi

#DEBHELPER#

exit 0
